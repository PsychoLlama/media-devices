#!/usr/bin/env bash
set -euo pipefail

date_string="$(date +%Y-%m-%d)"
short_rev="$(git rev-parse --short HEAD)"
version_string="${npm_package_version}-${date_string}.${short_rev}"

pnpm version --no-git-tag-version "${version_string}"
pnpm publish --tag rc --provenance --no-git-checks
